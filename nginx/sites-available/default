server {
    listen 127.0.0.1:8443;
    server_name example.com www.example.com;

    root /var/www/html;
    index index.nginx-debian.html;

    location / {
        try_files $uri $uri/ =404;
    }
    add_header Strict-Transport-Security "max-age=63072000" always;

    location = /3x-secret {
        return 301 /3x-secret/;
    }

    location ^~ /3x-secret/ {
        proxy_pass http://127.0.0.1:20530;

        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-Host $http_host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # часто полезно для панелей/вебсокетов
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }

    # настройка uri подписки panel/settings - enable subscription
    location = /sub-secret {
        return 301 /sub-secret/;
    }

    location ^~ /sub-secret/ {
        proxy_pass https://127.0.0.1:2096;

        proxy_set_header Host $host;
		proxy_set_header X-Forwarded-Host $http_host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # часто полезно для панелей/вебсокетов
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }

    #listen 443 ssl; # managed by Certbot
    #listen [::]:443 ssl ipv6only=on; # managed by Certbot
    # ssl_certificate /etc/letsencrypt/live/example.com/fullchain.pem; # managed by Certbot
    # ssl_certificate_key /etc/letsencrypt/live/example.com/privkey.pem; # managed by Certbot
    # include /etc/letsencrypt/options-ssl-nginx.conf; # managed by Certbot
    # ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem; # managed by Certbot

}
server {
    listen 80 default_server;
    listen [::]:80 default_server;

    server_name example.com www.example.com;

    location /.well-known/acme-challenge/ {
        root /var/www/html;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}
# временное решение, пока не настроили vless-tls и fallback
# server {
#     listen 8085 ssl;
#     listen [::]:8085 ssl;

#     server_name example.com www.example.com;

#     location = /3x-secret {
#         return 301 /3x-secret/;
#     }

#     location ^~ /3x-secret/ {
#         proxy_pass http://127.0.0.1:20530;

#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;

#         # часто полезно для панелей/вебсокетов
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection "upgrade";
#     }

#     ssl_certificate /etc/letsencrypt/live/example.com/fullchain.pem; # managed by Certbot
#     ssl_certificate_key /etc/letsencrypt/live/example.com/privkey.pem; # managed by Certbot
#     include /etc/letsencrypt/options-ssl-nginx.conf; # managed by Certbot
#     ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem; # managed by Certbot
# }